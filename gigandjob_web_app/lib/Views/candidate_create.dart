import 'package:flutter/material.dart';

class candidateCreate extends StatelessWidget {
  const candidateCreate({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text(
        "En construccion",
        textScaleFactor: 15.0,
      ),
    );
  }
}
